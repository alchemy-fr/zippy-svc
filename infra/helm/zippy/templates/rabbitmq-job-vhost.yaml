{{ $jobName := "rabbitmq-vhost-setup" }}
{{- with .Values.rabbitmq }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "zippy.fullname" $ }}-{{ $jobName }}-script
data:
  job.sh: |
    #!/bin/sh
    set -ex

    HOST="{{ include "zippy.fullname" $ }}-rabbitmq"
    n=0
    until [ "$n" -ge 20 ]; do
      rabbitmqctl -n rabbit@${HOST} await_startup && break
      n=$((n+1))
      sleep 5
    done

    rabbitmqctl -n rabbit@${HOST} await_startup
{{- if .enabled }}
    VHOST="{{ index .vhost }}"
    rabbitmqctl -n rabbit@${HOST} add_vhost ${VHOST}
    rabbitmqctl -n rabbit@${HOST} set_permissions -p ${VHOST} ${RABBITMQ_USER} '.*' '.*' '.*'
{{- end }}
{{- end }}
---
{{- with .Values.rabbitmq }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "zippy.fullname" $ }}-{{ $jobName }}
  annotations:
    "helm.sh/hook": post-install
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  template:
    spec:
      volumes:
      - name: scripts
        configMap:
          name: {{ include "zippy.fullname" $ }}-{{ $jobName }}-script
          defaultMode: 0777
      containers:
      - name: rabbitmq
        image: rabbitmq:3.7.14-management
        args: ["/setup/job.sh"]
        volumeMounts:
        - name: scripts
          mountPath: /setup/job.sh
          subPath: job.sh
        envFrom:
        - secretRef:
            name: {{ include "zippy.fullname" $ }}-rabbitmq
        - configMapRef:
            name: {{ include "zippy.fullname" $ }}-rabbitmq-config
      restartPolicy: Never
  backoffLimit: 10
{{- end }}

---
